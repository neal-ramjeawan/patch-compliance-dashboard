# Copy to secrets.toml for local testing (gitignored), or paste these
# same keys into Streamlit Community Cloud's "Settings > Secrets" UI
# when deploying — never commit real values.

AWS_ACCESS_KEY_ID = "AKIA..."
AWS_SECRET_ACCESS_KEY = "..."
AWS_DEFAULT_REGION = "us-east-1"
TABLE_NAME = "patch-dashboard-patch-data"
